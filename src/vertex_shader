#version 400 core

in vec3 position;
in vec2 uv;

//out vec3 color;
out vec2 out_uv;

void main(void) {
  gl_Position = vec4(position.xyz, 1.0);
  //color = vec3(position.x + 0.5,  1.0, position.y + 0.5);
  out_uv = uv;
}
